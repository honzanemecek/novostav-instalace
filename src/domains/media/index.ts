// Public API of the media domain.
// The domain currently exposes nothing at runtime: the <Media> render component
// is shared (used by pages, posts, and shared rich-text blocks), and the
// collection config is registered by src/payload/payload.config.ts via a deep
// import (build-time registration exemption).
export {}
